///they cannot be created in a class
library;

enum TextSizes {small, medium, large}
enum OrderStatus {processing, shipped, delivered}
enum PaymentMethods {oPay, bankTransfer, flutterWave, masterCard,}